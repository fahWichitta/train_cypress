{
  "name": "train_cypress",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run --spec cypress/e2e/Face*.cy.js",
    "query": "npx cypress run --spec cypress/e2e/query.js",
    "env:uat": "npx cypress run --env run_env=stg --spec 'cypress/e2e/verifyLoginPage.cy.js'",
    "env:api": "npx cypress run --env run_env=api --spec 'cypress/e2e/ws.js'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^13.11.0"
  },
  "dependencies": {
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-mongodb": "^6.2.0",
    "merge": "^2.1.1",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
